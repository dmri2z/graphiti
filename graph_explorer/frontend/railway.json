{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c '(cd ../backend && uv run uvicorn app:app --host 127.0.0.1 --port 8001) & npm run preview'"
  }
}
